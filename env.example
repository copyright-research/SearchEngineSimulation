# 复制此文件为 .env.local 并填入你的真实值

# Google Custom Search API Key
# 获取地址: https://console.cloud.google.com/apis/credentials
GOOGLE_API_KEY=your_google_api_key_here

# Google Search Engine ID (cx)
# 获取地址: https://programmablesearchengine.google.com/
GOOGLE_SEARCH_ENGINE_ID=your_search_engine_id_here

# GROQ API Key (用于 AI Overview 功能)
# 获取地址: https://groq.com
GROQ_API_KEY=your_GROQ_api_key_here

# Tavily API Key (用于增强搜索结果)
# 获取地址: https://tavily.com
# 可选：如果不设置，将只使用 Google Search
TAVILY_API_KEY=your_tavily_api_key_here

# rrweb Replay 访问密码（内部团队使用）
# 用于保护 /replay 路由，防止未授权访问
# 建议使用强密码，例如：SecureTeamPassword2024!
REPLAY_AUTH_TOKEN=your_secure_password_here

# Cloudflare R2 对象存储配置
# 用于上传和存储 rrweb 录制数据
# 获取地址: https://dash.cloudflare.com/ -> R2 -> Create Bucket
# 创建 API Token: Account -> R2 -> Manage R2 API Tokens
R2_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your_r2_access_key_id_here
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key_here
R2_BUCKET=your_r2_bucket_name_here

# Cron Job 密钥（用于保护定时任务）
# 用于验证 /api/cron/merge-recordings 的访问权限
# 建议使用随机生成的强密钥，例如：openssl rand -base64 32
CRON_SECRET=your_random_secret_here

# 数据库配置
# 本地开发：设置 USE_SQLITE=true 使用文件数据库（无需配置 Postgres）
# 生产环境：不设置 USE_SQLITE，使用 Vercel Postgres
USE_SQLITE=true

# Vercel Postgres 数据库连接（仅生产环境需要）
# 获取地址: https://vercel.com/dashboard -> Storage -> Postgres -> Create Database
# 创建数据库后，Vercel 会自动提供以下环境变量
# POSTGRES_URL=your_postgres_connection_string_here
# POSTGRES_PRISMA_URL=your_postgres_prisma_url_here
# POSTGRES_URL_NON_POOLING=your_postgres_non_pooling_url_here
# POSTGRES_USER=your_postgres_user_here
# POSTGRES_HOST=your_postgres_host_here
# POSTGRES_PASSWORD=your_postgres_password_here
# POSTGRES_DATABASE=your_postgres_database_here
